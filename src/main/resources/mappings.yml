mappings:
  - path: /api/auth/login
    method: POST
    soap:
      service: Auth
      operation: login
      wsdl: http://127.0.0.1:8080/services/Auth?wsdl
    request:
      "$.user": "$.login.user"
      "$.pass": "$.login.pass"
    response:
      "#root.return": "$.token"
